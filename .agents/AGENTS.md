# Custom Agent Rules for omertemo/my-blog

- **Git Operations**: Always ask the user for explicit confirmation in text before running any `git commit` or `git push` commands in the terminal. Do not run these commands automatically.
